include ../kitty.conf

# This window's identity, so KWin can find and position it later

# Run fastfetch on launch, close kitty when it exits isn't what we want —
# instead we loop it so the window stays open and refreshes
shell_integration disabled

include graphite-theme.conf
